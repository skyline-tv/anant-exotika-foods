# Public admin frontend variables only.
# In production, set these to the live HTTPS API and asset hosts.
# Never put database credentials, JWT secrets, admin passwords, or Razorpay secret keys here.
VITE_API_URL=http://localhost:5000/api/v1
VITE_ASSET_URL=http://localhost:5000
